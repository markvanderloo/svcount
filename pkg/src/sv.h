

/* Internally used index type.
 *
 * Note. C99 Standard Section 6.5.3.4 -- The sizeof operator,
 * implies that size_t must be able to hold the size of the largest 
 * array supported by the implementation. 
 *
 */
typedef size_t idx;


